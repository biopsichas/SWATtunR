% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_cal_val}
\alias{plot_cal_val}
\title{Plot Calibration and Validation Performance Indexes in Boxplots}
\usage{
plot_cal_val(cal_df, val_df)
}
\arguments{
\item{cal_df}{A data frame containing calibration results. Must include a \code{run}
column and one or more performance metric columns.}

\item{val_df}{A data frame containing validation results. Must include a \code{run}
column and one or more performance metric columns that match those in \code{cal_df}.}
}
\value{
A \code{ggplot} object showing faceted boxplots and jittered values for each
performance metric, comparing calibration vs. validation results.
}
\description{
Creates comparative boxplots and jittered points for calibration and validation
performance metrics. The function takes two data frames: one for calibration and
one for validation—with a column \code{run} and additional performance metric columns.
It reshapes the data, adds identifiers for type, and produces faceted plots for
each metric.
}
\examples{
\dontrun{
cal <- data.frame(run = 1:10, NSE = runif(10), KGE = runif(10))
val <- data.frame(run = 1:10, NSE = runif(10), KGE = runif(10))
plot_cal_val(cal, val)
}

}
\keyword{plot}
