% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_pcp_vs_flow}
\alias{plot_pcp_vs_flow}
\title{Plot Precipitation vs. Runoff}
\usage{
plot_pcp_vs_flow(
  pcp_file,
  flow,
  x_label = "Date",
  y_left = "Runoff (m3/s)",
  y_right = "Precipitation (mm)",
  title = "Precipitation and Runoff"
)
}
\arguments{
\item{pcp_file}{Character string specifying the path to the precipitation data file.
The file should contain columns: year (integer), yday (integer), pcp (numeric, in mm).}

\item{flow}{Data frame with at least two columns: \code{date} (Date) and \code{discharge} (numeric, in m³/s).}

\item{x_label}{Character string for x-axis label. Defaults to "Date".}

\item{y_left}{Character string for left y-axis label (runoff). Defaults to "Runoff (m³/s)".}

\item{y_right}{Character string for right y-axis label (precipitation). Defaults to "Precipitation (mm)".}

\item{title}{Character string for plot title. Defaults to "Precipitation and Runoff".}
}
\value{
A ggplot2 object representing the dual-axis plot.
}
\description{
Creates a dual-axis plot comparing precipitation and runoff over time.
}
\examples{
\dontrun{
pcp_file_path <- "path/to/my_station.pcp"
flow_data <- data.frame(date = as.Date("2023-01-01") + 0:364, discharge = runif(365, 0, 100))
plot_pcp_vs_flow(pcp_file_path, flow_data)
}
}
\keyword{plot}
